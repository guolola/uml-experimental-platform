// Keeps the historical app import path while workspace module contracts live with workspace entities.
export {
  WORKSPACE_MODULES,
  SHELL_ROUTE_MODULES,
  assertUniqueWorkspaceModules,
  findShellRouteModule,
  type ShellRoutePath,
  type WorkspaceModuleDefinition,
} from "../entities/workspace/modules";
